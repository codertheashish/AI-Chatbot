# Core web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Templating & static/multipart form support
jinja2==3.1.4
python-multipart==0.0.9
aiofiles==23.2.1

# HTTP client for OpenRouter streaming
httpx==0.27.2

# Config
python-dotenv==1.0.1

# Numerics (embeddings / vector search fallback)
numpy==1.26.4

# Document parsing
pypdf==4.3.1
python-docx==1.1.2

# Vector search (optional but recommended). If your deployment target has a
# package-size limit and the build fails, comment this line out -- app.py
# automatically falls back to a pure NumPy cosine-similarity search.
faiss-cpu==1.8.0.post1
